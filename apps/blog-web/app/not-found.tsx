import { GlobalNotFound } from "@/components/layout/global-not-found";
import { NOT_FOUND_METADATA } from "@/lib/notFoundMetadata";

export const metadata = NOT_FOUND_METADATA;

export default GlobalNotFound;
